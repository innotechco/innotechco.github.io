---
name: bardia
description: Describe what this custom agent does and when to use it.
tools: Read, Grep, Glob, Bash # specify the tools this agent can use. If not set, all enabled tools are allowed.
---

<!-- Tip: Use /create-agent in chat to generate content with agent assistance -->

Define what this custom agent does, including its behavior, capabilities, and any specific instructions for its operation.

میخوام تو صفحات partners پایین صفحاتشون که چندین تا کارت هست تغییر کنه سایزش و منطقش که کاربر روی وکتور میزنه و تکون میخوره رو دست نزن صرفا برای صفحات مختلف پارترنز میخوام تعدادشونو بیشتر یا کمتر کنیم برای صفحه آیتونیکس بزار همون شکلی بمونه برای صفحه brightidea 6 تا کارت باید باشه برای صفحه lean 6 تا کارت باید باشه برای صفحه alleantia باید 4 تا صفحه باشه برای trex باید 5 تا کارت باشه برای idc باید 5 تا کارت باشه برای startin باید 8 تا کارت باشه و فعلا محتویاتی که هست رو بزار و اگر تعدادشون زیاد بود تکراری بزار ولی میخوام هرکارت مستقل باشه میخوام جوری باشه که اطلاعات متن جدا باشه و عکس های اس وی جی جدا باشه که بتونم تغییر بدم یا اضافه کنم میخوام اسم آیدی و تایتل ها یکی باشن میخوام یه عکسی که بغل متن World's #1 Innovation Cloud هست اون عکس سمت راست به صورت پابلیکه و همه صفحات دارن استفاده میکنن فقط برای آیتونیکس باشه صفحات دیگه هم استفاده کنند مشکلی نیست ولی چون قراره هر صفحه عکس خودشو داشته باشه و متفاوت باشه میخوام توی دیتا جی اس هرکودوم مثلا ایمپورت داشته باشن هم برای عکس هم svg ک میخوام همشو تغییر بدم کارم راحت باشه و صرفا با تغییر اسم عکسا تغییر کنه و اینکه میخوام منطق کارت که وقتی کاربر روی وکتور میزنه و تکون میخوره اشتباه کار نکنه حتی برای تعداد کارت های بیشتر و کمتر یعنی اگر مشکلی بود درستش کن کلا هدف این کار اینه که هر صفحه عکس و اس وی جی و متناش مستقل باشن تا من راحت تر بتونم محتویاتشونو تغییر بدم بدون مشکل سایز عکسا و کارت و اس وی جی رو تغییر نده یا متنی رو عوض نکن صرفا اگر چیز زیادی درست کردیم مثل کارت از همون کارت هایی ک داریم متن و عکسشو استفاده بکن ولی جدا جدا باشند تا بتونم بعدا عوضشون کنم و عکس بالای صفحات که لوگو گذاشتیم میخوام سایزشون بزرگ باشه مثل brightidea یا همون حدودا به عنوان مثال

