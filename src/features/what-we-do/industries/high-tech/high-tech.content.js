import {getIndustryPage} from "../industryContent.js";

export const {
  slug,
  hero,
  accelerationParagraphs,
  capabilities,
  ecosystemCards,
  liveInsights,
  sectionLabels,
} = getIndustryPage("high-tech");
